# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# CADENCE experiment output. Run-output paths only: a committed golden event stream
# (e.g. an M1 test fixture) must not vanish just because it happens to be a .parquet file.
studies/**/runs/
